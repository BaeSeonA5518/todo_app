rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 개발·학습용: 누구나 읽기/쓰기 가능. 배포 전에는 반드시 인증 등으로 좁히세요.
    match /todos/{todoId} {
      allow read, write: if true;
    }
  }
}
